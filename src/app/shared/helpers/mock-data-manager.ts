// File to generate the data for one month and save it in a json file.
/*
 *  Them mock has the following structure:
 *  const monthData = [
 *   {
 *     time: number // timestamp,
 *     value: string // some text
 *   }
 * ]
 * */

// it generates the intervals based on 1 day
export function generateMockDataForOneDay(recordsAmount) {
  let records = []
  for (let a = 0; a < recordsAmount; a++) {}
}

// it generates the intervals based on 1 month
